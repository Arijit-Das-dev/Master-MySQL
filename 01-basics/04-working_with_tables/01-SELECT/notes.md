## WORKING WITH TABLES - SELECT

### QUERY - SELECT
```sql
-- SQL COMMANDS

SELECT * FROM students;                 -- selects all columns
SELECT id, name, dept FROM students;    -- selects specific columns
```